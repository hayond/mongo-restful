plugins {
    id("io.micronaut.application") version "3.6.7"
}

version = "0.1"
group = "site.hayond.revolution"

repositories {
    mavenCentral()
}

dependencies {
    // Fix 'warning: unknown enum constant When.MAYBE'
    compileOnly("com.google.code.findbugs:jsr305")
    // Problem
    compileOnly("io.micronaut.problem:micronaut-problem-json")
    compileOnly("io.micronaut:micronaut-http")
    // Exception
    compileOnly("io.micronaut:micronaut-http-server")
    compileOnly("org.mongodb:mongodb-driver-core")
}

java {
    sourceCompatibility = JavaVersion.toVersion("17")
    targetCompatibility = JavaVersion.toVersion("17")
}

micronaut {
    processing {
        incremental(true)
        annotations("site.hayond.*")
    }
}
